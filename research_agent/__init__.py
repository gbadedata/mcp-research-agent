"""mcp-research-agent: an AI automation that collects information from web sources and
produces a digest, built as an MCP server plus a tool-using Claude agent.

tools -> (MCP server | agent loop) -> store -> digest.
"""
__version__ = "0.1.0"
